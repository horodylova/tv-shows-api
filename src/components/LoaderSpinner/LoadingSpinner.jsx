import {Container, Loader } from './LoadingSpinner.styled'

function LoadingSpinner() {
    return (
        <Container>
             <Loader />
        </Container>
      );
}

export default LoadingSpinner